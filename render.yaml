services:
  - type: web
    name: algosns-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      - key: SOLAR_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: https://algosns-web.onrender.com

  - type: web
    name: algosns-web
    runtime: static
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://algosns-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
